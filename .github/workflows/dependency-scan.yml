# Dependency Scan — RFC RAG
#
# Generated for ci-cd-architect standard v1.2.0, Rule CI-CDW-71.
# Checks NuGet packages for known vulnerabilities (Critical/High → fail)
# and deprecated packages (advisory, non-blocking).
#
# Ported from k8s-toolkit dependency-scan.yml.

name: Dependency Scan

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_NOLOGO: true

jobs:
  nuget-vulnerability-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Set up .NET
        uses: actions/setup-dotnet@9a946fdbd5fb07b82b2f5a4466058b876ab72bb2  # v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore RfcRag.slnx

      - name: Check NuGet package vulnerabilities
        run: |
          dotnet list RfcRag.slnx package --vulnerable --include-transitive 2>&1 | tee vuln-report.txt
          if grep -q 'Critical\|High' vuln-report.txt; then
            echo "FAIL: Critical or High severity NuGet vulnerabilities found."
            cat vuln-report.txt
            exit 1
          fi

      - name: Check deprecated NuGet packages (advisory)
        continue-on-error: true
        run: dotnet list RfcRag.slnx package --deprecated --include-transitive

      - name: Upload vulnerability report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: nuget-vuln-report
          path: vuln-report.txt
